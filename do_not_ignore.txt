🔍 Why these?

opencv-python → camera + rendering
mediapipe → hand tracking + handedness
numpy → vector math (angles, normalization)
pyautogui → cursor, click, drag, scroll

⚠️ Notes (important)

numpy==1.26.4 → avoids conflicts with MediaPipe
Do NOT add TensorFlow manually (MediaPipe bundles what it needs)
Works fine in your virtual environment

📦 Install command

pip install -r requirements.txt
